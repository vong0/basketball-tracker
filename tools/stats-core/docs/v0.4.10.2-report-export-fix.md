# v0.4.10.2 Report Export Fix

- Fixed exported interactive report so dropdowns update the report after download.
- Exported HTML is report-only: tracking, quality, guides, backup/data tabs are hidden.
- Exported report uses forced desktop layout on phones.
- Fixed report-export script injection so it targets the real closing body tag and avoids script parsing problems.
