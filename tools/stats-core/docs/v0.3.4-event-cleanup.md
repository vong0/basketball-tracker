# v0.3.4 Event cleanup

Changes:

- Removed the quick event preset button grid under the Add Event button.
- Event entry now uses the player/related player + custom type/subtype dropdowns only.
- Kept dynamic subtype options and usage notes for each valid event combination.
- Removed deep top 3 as a zone; imported `deep top 3` values normalize to `top 3`.
- Updated standalone storage key so this version opens cleanly from earlier test versions.

Next build target:

- Start improving presentation/report pages: game report, player advanced stats, team stats, and shot chart sections in a layout closer to the Spartans website mockups.
