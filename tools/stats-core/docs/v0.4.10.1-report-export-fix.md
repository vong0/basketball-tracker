# v0.4.10.1 Interactive report export fix

Fixes a page-breaking HTML parser bug caused by an unescaped closing script tag inside the report-export builder string.

Use `standalone/basketball-tracker-standalone-v0.4.10.1-interactive-report-fixed.html`.

The interactive report export remains report-only and desktop-forced on phones.
