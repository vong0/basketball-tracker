# v0.4.1 Report Layout Update

Changes:
- Shot list stretches to match shot map height.
- Zone and contest breakdowns use FG as FGM/FGA instead of separate FGM/FGA columns.
- Score / points card layout cleaned up.
- Selected player advanced stats changed to compact cards.
- All players table has sticky player column.
- Event breakdown table added so tracked Events data is more visible in reports.
